[module/hostname]
type = custom/script
interval = 256

format = "<label> "
#format-prefix = " "
;format-prefix-foreground = ${colors.foreground-alt}

exec = echo "$(uname -n)"

; vim:fenc=utf-8:ft=dosini:
